# Data source: 'sim' uses built-in fixture animation, 'bridge' connects to Python bridge
VITE_DATA_SOURCE=sim

# WebSocket URL for the Python bridge (only used when VITE_DATA_SOURCE=bridge)
VITE_BRIDGE_WS=ws://localhost:8771
